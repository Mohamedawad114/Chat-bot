export enum Sys_Role {
  user = 'user',
  assistant = 'assistant',
}

export enum Gender {
  male = 'male',
  female = 'female',
}
export enum Provider {
  google = 'google',
  system = 'system',
}

export enum ChatFor{
  socket='socket',
  request="request"
}