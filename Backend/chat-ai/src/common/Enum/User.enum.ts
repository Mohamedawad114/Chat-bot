export enum Sys_Role {
  user = 'user',
  admin = 'admin',
  company_admin = 'company_admin',
}

export enum Gender {
  male = 'male',
  female = 'female',
}
export enum Provider {
  google = 'google',
  system = 'system',
}
